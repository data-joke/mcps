# MySQL MCP 连接配置样例（仅供参考，请按实际调整）
# 连接凭据必填、无默认值——开源分享后每个人配置不同，必须由环境变量提供。
# 推荐方式：把变量写到 Claude 配置（~/.claude.json）mcpServers.env 中；
# 本文件仅作为本地调试时的备选方案（拷贝为 .env 使用），不会覆盖 Claude 注入的同名变量。

# 必填（缺失会报错）：MySQL 服务地址 / 账号 / 密码
MYSQL_HOST=127.0.0.1
MYSQL_USER=root
MYSQL_PASSWORD=your_password

# 选填：默认库（不填则跨库查询）
MYSQL_DATABASE=your_database

# 选填（均有默认值）
MYSQL_PORT=3306
MYSQL_CHARSET=utf8mb4
MYSQL_CONNECT_TIMEOUT=10
MYSQL_READ_TIMEOUT=60

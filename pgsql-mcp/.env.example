# pgsql MCP 连接配置样例（仅供参考，请按实际调整）
# 连接凭据必填、无默认值——开源分享后每个人配置不同，必须由环境变量提供。
# 推荐方式：把变量写到 Claude 配置（~/.claude.json）mcpServers.env 中；
# 本文件仅作为本地调试时的备选方案（拷贝为 .env 使用），不会覆盖 Claude 注入的同名变量。

# 方式一（必填）：完整连接字符串（推荐）
PG_CONNECTION_STRING=postgresql://user:password@localhost:5432/postgres

# 方式二：使用拆分字段（与 PG_CONNECTION_STRING 二选一，都用时以连接字符串为准）
# PG_HOST=localhost
# PG_PORT=5432
# PG_USER=user
# PG_PASSWORD=password
# PG_DATABASE=postgres
